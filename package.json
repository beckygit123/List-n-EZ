{
  "name": "lystynz-ez",
  "version": "1.0.0",
  "description": "AI assistant for reselling success.",
  "private": true,
  "scripts": {
    "install": "npm install --prefix server && npm install --prefix app",
    "dev": "concurrently \"npm start --prefix server\" \"npm run dev --prefix app\"",
    "build": "npm run build --prefix app"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1"
  }
}
